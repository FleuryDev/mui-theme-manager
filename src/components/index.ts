/**
 * Exports the `SwitchThemeMode` component as the default export from the `./Buttons/SwitchThemeMode` module.
 * 
 * The `SwitchThemeMode` component is likely used to toggle between different theme modes (e.g., light and dark mode)
 * in the application.
 */
export { default as SwitchThemeMode } from './Buttons/SwitchThemeMode';
export { default as ThemeList } from './List/ThemeList';